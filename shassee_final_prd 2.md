# وثيقة متطلبات المنتج الفنية والتشغيلية (PRD)
## منصة "شاصي" لذكاء وفحص السيارات المستوردة (Shassee AI)
### إصدار المنصة الاستشارية وحماية الاشتراكات - لعام 2026

---

## 1. الرؤية العامة ونموذج العمل (Executive Vision & Business Model)

منصة **"شاصي" (Shassee AI)** هي منصة استشارية فنية ومالية رقمية متكاملة موجهة للسوق الليبي، تهدف إلى كسر ركود سوق السيارات المستوردة وبناء الثقة عبر تقديم **"ملف السيارة الموثق"**. 

بناءً على الواقع الفعلي لعمليات الاستيراد في ليبيا، **لا تتدخل المنصة في المزايدة أو الشحن الفعلي أو التجارة المباشرة لقطع الغيار**. بدلاً من ذلك، تعمل المنصة كـ **"مفتش ومثمن رقمي مستقل"** يقدم تقارير فحص بالذكاء الاصطناعي وتقديرات لوجستية واقعية لقطع الغيار والتأهيل مقابل رسوم خدمة أو اشتراكات ذكية محمية برمجياً ضد إساءة الاستخدام ومشاركة الحسابات.

---

## 2. الهيكل التقني المقترح (System Architecture & Tech Stack)

يُطلب من المهندس المطور بناء المنصة كـ **موقع ويب متفاعل متجاوب بالكامل (Web Application)** محسن للعمل على الآيباد والهواتف الذكية دون الحاجة لتطبيقات متاجر Apple أو Google، بالاعتماد على الهيكل التالي:

*   **الواجهة الأمامية (Frontend):** **React.js** أو **Next.js** مع مكتبة **Tailwind CSS** لدعم واجهات مستخدم سريعة باللغة العربية كخط أساسي (RTL).
*   **الواجهة الخلفية (Backend):** **FastAPI (Python)** لسهولة ربط ومعالجة خوارزميات الذكاء الاصطناعي ومعالجة البيانات الجغرافية.
*   **قاعدة البيانات والمصادقة (Database & Auth):** **Supabase (PostgreSQL)** لإدارة الجداول، أمن الجلسات، وحفظ تقارير الفحص الفورية.
*   **محرك الذكاء الاصطناعي (AI Engine):** الربط السحابي بـ **OpenAI GPT-4o Vision API** لتحليل الصور وتقدير الأضرار التنبؤية.
*   **بوابة البيانات الخارجية (Auction Data API):** الاشتراك في بوابات وسيطة مرخصة (مثل **BidCars API** أو **AutoBidMaster API**) لجلب بيانات وتصفح سيارات كوبارت وIAAI بـ "شخصية وألوان شاصي" دون تداخل قانوني أو مخاطرة مزايدة.

---

## 3. الميزات والوظائف البرمجية الأساسية (Functional Modules)

### الميزة الأولى: تصفح المزادات المغلف (Auction Wrapper & Integrated Search)
*   **الوصف:** يتصفح الزبون سيارات كوبارت وIAAI من داخل موقع "شاصي" مباشرة بواجهة مستوحاة من هويتك البصرية (الأزرق الملكي والأخضر الزمردي).
*   **التطبيق البرمجي:** 
    1. يستدعي الموقع محرك البحث والفلاتر (الماركة، الموديل، السنة) عبر الـ API الخاص بالبوابة الوسيطة.
    2. يعرض النتائج منسقة بهوية شاصي.
    3. عند الضغط على سيارة، يظهر خياران: معلومات عامة مجانية، أو **فتح تقرير فحص شاصي الذكي (المدفوع)**.
    4. يوضع زر واضح: **"انتقل للمزايدة على صفحة السيارة الأصلية في Copart"** لفتح رابط خارجي في متصفح العميل لإتمام المزايدة بحسابه الخاص.

### الميزة الثانية: كاشف الأعطال البصري والتحليل التنبئي للهيكل (AI Visual & Frame Assessment)
*   **الوصف:** يقوم بتحليل صور السيارة من ساحة المزاد بالذكاء الاصطناعي لتقدير تضرر الشاصي الخفي ومحاكاة الهيكل.
*   **التطبيق البرمجي:**
    1. يرسل النظام صور السيارة لـ GPT-4o Vision API مع توجيه فني (Prompt) سري ومقفل.
    2. يولد تقريراً فنياً باللغة العربية يشخص حالة الصدمة والقطع المتضررة بدقة.
    3. **المجسم التفاعلي (3D Heatmap):** باستخدام مكتبة **Three.js**، يتم عرض مجسم سيارة هيكلي ثلاثي الأبعاد على الآيباد يتغير تلوينه تلقائياً حسب زاوية الصدمة المكتشفة بالذكاء الاصطناعي (🔴 للضرر الهيكلي، 🟡 للقطع الاستهلاكية المعوجة، 🟢 للسليم).

### الميزة الثالثة: حاسبة الشحن ومؤشر الصدأ الجغرافي (Logistics & Rust Indicator)
*   **الوصف:** تحديد تكلفة الشحن اللوجستية بدقة وحماية العميل من صدأ السيارات الشتوي.
*   **التطبيق البرمجي:**
    1. **حاسبة النقل البري والبحري:** يقوم الموقع بقراءة "موقع السيارة/الولاية" من بيانات المزاد، ومطابقتها مع جدول أسعار النقل البري والبحري لليبيا (يتم تحميله وتحديثه في Supabase بالتنسيق مع شركات الشحن) لعرض قيمة النقل الحقيقية للعميل.
    2. **مؤشر الصدأ:** يتحقق التطبيق من تاريخ تسجيل السيارة في ولايات "حزام الملح" الأمريكي (Salt-Belt) مثل ميشيغان ونيويورك؛ وإذا كانت السيارة مسجلة هناك، يظهر تحذير فوري: *"خطر صدأ مرتفع 75% في قواعد التعليق والهيكل"* لإشعار المشتري قبل المزايدة.

### الميزة الرابعة: مصفوفة قطع الغيار المقارنة (Hybrid Parts Cost Matrix)
*   **الوصف:** بعد حصر الذكاء الاصطناعي للقطع التالفة، يعرض الموقع خيارات تسعير واضحة تتيح للعميل تحديد ميزانيته:
*   **التطبيق البرمجي:** يربط المطور القطع بقاعدة بيانات الموقع لتعرض تكلفتين:
    *   **الخيار الأول (مستعمل تفصيخ من أمريكا):** جلب السعر من مواقع السكراب الأمريكية (مثل LKQ أو Car-Part API) مع إضافة كلفة النقل للمستودع، مع إشارة بأن الشحن البحري لليبيا مجاني عبر الشحن المتداخل (Piggyback) داخل السيارة.
    *   **الخيار الثاني (جديد من دبي):** جلب السعر بربط مع منصات دبي (مثل PartsSouq API) لعرض أسعار: أصلي OEM، تايواني، وصيني مع كلفة الشحن الجوي أو البحري لليبيا.
    *   **السوق الليبي:** عرض سعر مقارن استرشادي بالدينار الليبي يتم إدخاله وتحديثه يدوياً في لوحة تحكم الإدارة.

### الميزة الخامسة: ملف السيارة الموثق والـ QR Code (Digital Car Ledger)
*   **الوصف:** أرشفة وحفظ التقرير النهائي للسيارات برابط دائم وموثق لكسر ركود السوق.
*   **التطبيق البرمجي:**
    1. عند الانتهاء من الفحص، يحفظ التقرير في Supabase مع رقم الشاصي (VIN).
    2. يولد النظام كود **QR Code** فريد يطبع ويوضع على زجاج السيارات في المعارض الليبية.
    3. يتيح مسح الكود بكاميرا الهاتف لأي زبون فتح التقرير والاطلاع على أمانة ونظافة السيارة فحصاً وميزانية (دون إخفاء للحوادث).

---

## 4. نظام حماية الاشتراكات والأمان لمنع مشاركة الحسابات (Security & Monetization)

لمواجهة إساءة استخدام الاشتراكات ومشاركتها بين تجار المعارض، يلتزم المطور ببرمجة الضوابط الصارمة التالية:

### أولاً: كوتا التقارير الباقية (Quota-Based Billing)
*   لا يفتح العميل تقريراً إلا باستهلاك رصيد تقارير مسجل في حسابه بـ Supabase.
*   **الباقات:** باقة 3 تقارير، باقة 10 تقارير، إلخ.
*   بمجرد الضغط على "توليد التقرير"، يخصم رصيد (1) تلقائياً، وإذا وصل لـ (0)، يقفل الزر حتى إعادة الشحن.

### ثانياً: تقييد الجلسة النشطة الواحدة (Single Active Session)
*   يُبرمج الموقع ليتيح **جلسة نشطة واحدة فقط لكل حساب في نفس اللحظة**.
*   إذا حاول مستخدم آخر فتح الحساب من هاتف أو آيباد مختلف، يتم طرد الجلسة الأولى فوراً (Auto-Logout) مع إشعار: *"تم تسجيل الدخول من جهاز آخر، تم قفل الجلسة الحالية حمايةً لحسابك"*.

### ثالثاً: بصمة المتصفح المعتمد والجهاز المقفل (Browser Fingerprinting & MFA Device Lock)
*   عند الاشتراك في الباقة الدائمة، يتم التقاط بصمة المتصفح الفريدة لجهاز العميل (نوع الجهاز، نظام التشغيل، دقة المتصفح).
*   إذا تم تسجيل الدخول من متصفح أو جهاز غير معروف لا يملك مفتاح التحقق السري (Authorized Token/Cookie)، يطالب الموقع بإدخال رمز تحقق (OTP) يرسل حصراً لهاتف المشترك عبر (الواتساب أو SMS)، مع قفل تفعيل المتصفح السابق تلقائياً لحظر الاستخدام المشترك والواسع.

---

## 5. مخطط قاعدة البيانات السحابية الجاهز (Supabase SQL Schema)

يجب على المطور تهيئة قاعدة البيانات في Supabase ورفع الجداول والسياسات الأمنية كالتالي:

```sql
-- 1. جدول المستخدمين والاشتراكات والكوتا
CREATE TABLE profiles (
    id UUID REFERENCES auth.users ON DELETE CASCADE PRIMARY KEY,
    username TEXT UNIQUE,
    phone_number TEXT,
    remaining_reports INT DEFAULT 0, -- رصيد التقارير المتبقي للعميل
    subscription_type TEXT DEFAULT 'free', -- نوع الاشتراك: مجاني، باقة، دائم
    created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL
);

-- 2. جدول الأجهزة وبصمات المتصفح المعتمدة للحماية
CREATE TABLE authorized_devices (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id UUID REFERENCES profiles(id) ON DELETE CASCADE,
    device_fingerprint TEXT NOT NULL,
    authorized_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL,
    CONSTRAINT unique_user_device UNIQUE (user_id, device_fingerprint)
);

-- 3. جدول تقارير فحص السيارات الموثقة
CREATE TABLE inspection_reports (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    vin VARCHAR(17) NOT NULL,
    lot_number VARCHAR(50),
    vehicle_make TEXT,
    vehicle_model TEXT,
    vehicle_year INT,
    source_auction TEXT DEFAULT 'Copart',
    damage_description TEXT, -- تقرير الذكاء الاصطناعي البصري
    frame_damage_probability NUMERIC, -- احتمالية تضرر الشاصي
    inland_shipping_cost NUMERIC, -- كلفة النقل البري التقديرية
    ocean_shipping_cost NUMERIC, -- كلفة الشحن البحري التقديرية
    estimated_parts_json JSONB, -- مصفوفة أسعار قطع الغيار المقارنة (أمريكا، دبي، ليبيا)
    qr_code_url TEXT, -- رابط ملصق الـ QR المولد للسيارة
    created_by UUID REFERENCES profiles(id),
    created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL
);

-- 4. تفعيل سياسات الأمان والحوكمة (Row Level Security)
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE authorized_devices ENABLE ROW LEVEL SECURITY;
ALTER TABLE inspection_reports ENABLE ROW LEVEL SECURITY;

-- السماح للعامة بمسح وقراءة التقارير (لقراءة الـ QR Code في المعارض الليبية)
CREATE POLICY "Allow public read of inspection reports" ON inspection_reports
    FOR SELECT USING (true);

-- السماح للمشتركين وصانعي التقارير بحفظ وإدراج تقاريرهم الخاصة فقط
CREATE POLICY "Allow users to insert their own reports" ON inspection_reports
    FOR INSERT WITH CHECK (auth.uid() = created_by);
```

---

## 6. واجهة وتجربة المستخدم (UI/UX Specifications)

*   **الألوان والهوية البصرية الهندسية:**
    *   **الأزرق الملكي الداكن (#1E3A8A):** للواجهات، يرمز للثقة والأمان والمصداقية الاستشارية.
    *   **الأخضر الزمردي (#10B981):** للتأكيد على الجدوى والقطع السليمة والأرباح.
    *   **الأصفر والأحمر الكهرماني (#F59E0B / #EF4444):** لتنبيهات الصدأ وحزام الملح ومخاطر الشاصي الملتوي.
*   **توافق الأجهزة:** تصميم واجهة ويب متجاوبة بالكامل (RTL) تناسب شاشة الآيباد Pro M1 الخاص بالمدير والمستثمر وشاشات هواتف الزبائن في المعارض الليبية لضمان الفحص الميداني السريع.
